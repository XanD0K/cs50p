# Demonstrates a constant

# It's a convention to (at least) capitalize constant variables
# In Python, there is nothing preventing that "constant" variable to be changed
MEOWS = 3

for _ in range(MEOWS):
    print("meow")
